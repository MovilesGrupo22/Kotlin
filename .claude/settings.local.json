{
  "permissions": {
    "allow": [
      "Bash(./gradlew signingReport)",
      "Bash(keytool -list -v -keystore \"C:/Users/nicol/.android/debug.keystore\" -alias androiddebugkey -storepass android -keypass android)"
    ]
  }
}
